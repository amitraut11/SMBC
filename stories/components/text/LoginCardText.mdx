import { Meta, Story } from '@storybook/addon-docs';
import { Default as LoginCardText } from './LoginCardText.stories';

<Meta title="Components/Text/LoginCardText" />

# Login Card Text

The `LoginCardText` component is designed to display instructional text for login forms.

## Usage

You can use the `LoginCardText` like this:

```tsx
<LoginCardText 
  text="Please log in." 
  textColor="#000" 
  fontSize="14px" 
/>
